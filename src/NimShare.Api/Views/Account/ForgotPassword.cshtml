@model NimShare.Api.Controllers.ForgotPasswordViewModel
@{ ViewData["Title"] = T["forgot.title"]; Layout = "_Layout"; }
<div class="landing-shell">
    <div class="landing-card" style="max-width: 480px">
        <div style="text-align:center;margin-bottom:var(--sp-4)">
            <img src="~/img/logo-400.png" srcset="/img/logo-400.png 1x, /img/logo-800.png 2x"
                 alt="NimShare" style="max-width:280px;width:100%;height:auto;margin:0 auto"/>
        </div>
        <h1 style="text-align:center">@T["forgot.title"]</h1>
        <p class="landing-meta" style="text-align:center">@T["forgot.lede"]</p>

        <form method="post" class="stack" style="margin-top: var(--sp-6)">
            <div asp-validation-summary="All" class="helper" style="color:var(--status-critical)"></div>
            <div class="field">
                <label for="Email">@T["auth.email"]</label>
                <input type="email" name="Email" id="Email" required autocomplete="email" value="@Model.Email"/>
            </div>
            <button type="submit" class="btn btn-primary">@T["forgot.button"]</button>
        </form>

        <div style="margin-top:var(--sp-4);text-align:center">
            <a href="/login" class="muted" style="font-size:.85rem">@T["forgot.back_to_login"]</a>
        </div>
    </div>
</div>
